{
  "name": "@landfall/edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist -c ../../../wrangler.jsonc",
    "deploy": "wrangler deploy -c ../../../wrangler.jsonc",
    "dev": "wrangler dev -c ../../../wrangler.jsonc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@landfall/core": "workspace:*",
    "@landfall/server": "workspace:*",
    "@landfall/web": "workspace:*",
    "drizzle-orm": "^0.44.0",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260908.1",
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
