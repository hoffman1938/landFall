# Remediation Decisions Log

One line per deviation or judgment call made while executing the remediation program
(README.md at repo root). Precedence order applied: Identity Freeze (§2) → program document →
existing docs → existing code.

| # | Phase/Task | Decision | Why |
|---|---|---|---|
| 1 | R0/A2 | "≥×2 on roughly 1 round in 10" relaxed to "storm bonus (M>1) on ~1 in 11.3, ×2+ on ~1 in 131, via a ×1.25 Category 2". | Mathematically infeasible as written: the funding invariant (which the task itself declares as the invariant replacing E[M]=1) caps E[M−1] at `0.25×0.12/0.88 = 3/88 ≈ 0.0341`, while P(M≥2)=0.1 alone forces E[M−1] ≥ 0.1. Max feasible P(M≥2) is ~1 in 29 with zero tail. The invariant (bankroll protection, the panel's core A-workstream complaint) wins; the *felt* bonus frequency the constraint was after ("felt frequency of storm bonus") is delivered at ~1 in 11 by Category 2 ×1.25, keeping the funded ×100/×500 tail. |
| 2 | R0/A2 | Perfect Storm kept at nominal ×500 with count 1 in 2²⁰ (~1 per 1.05M rounds), rather than a fatter ×200 tier. | A2 allows "×200–×500 nominal given A3's cap"; ×500 preserves the marketable dream while A3's 25×-handle clamp bounds the real liability. Budget slack after all tiers: 61 units in 2²⁰ space. |
| 3 | R0/A3 | Reserve may go negative on an early big storm (house backstops); ledger records it rather than blocking the payout. | Blocking or shrinking a disclosed payout at settle time would be a worse integrity breach than a temporary reserve deficit; expected drift is positive (funding invariant with 99.8% utilization) so the fund self-heals. Insolvency policy documented in G2. |
| 4 | R0/A1 | Rounding dust from the rake split (floor on surge and reserve shares) accrues to the house share. | Deterministic, ≤2 minor units per round, and keeps `houseRake + surge + reserve === rakeMinor` exact in integer math. |
| 5 | R0/A1 | Rounds table now records `rake_bp`, `max_payout_multiple`, `power_capped` at settlement. | Verification of historic rounds must use the economy config in force *then*, not the live one; /api/round serves these with a fallback to the live config for legacy rows. |
| 6 | R0 | Portable Node.js v22.23.1 (official nodejs.org zip) installed to `C:\Users\gtsulaia\tools\` — no system-wide install. | The machine had no Node toolchain; the program mandates a green suite after every task. Zip distribution avoids admin rights and system changes. |
| 7 | R0/A3 | `settleRound` clamp floor: the cap never cuts below `distributable` (the ×1 pari-mutuel base). | Identity Freeze §2.2/§2.6 — survivors' pari-mutuel share is inviolate; the cap only limits the *reserve-funded bonus*. |
| 8 | R1/A4 | Flat-odds Golden Anchor gives equal odds per surviving **stake entry**, not per player (a Split fleet holds 2 entries → 2 tickets). | The public lock snapshot contains stake entries, not player identities; per-player flat odds would need player grouping in public data, breaking the "recompute from the snapshot" verification property (Identity Freeze §2.3). Feature is P2, default-off (`SURGE_FLAT_ODDS_EVERY_N = 0`) pending RG/compliance review. |
| 9 | R1/B1 | Receipts issued on **all** anchor/fleet/cancel decisions (not only late rejections), incl. INSUFFICIENT/BAD_SPLIT. | Complete audit is simpler to reason about than selective; the mandated fog-boundary class is a subset. Receipt key is a separate server secret (`server_secrets`) — never the fairness chain terminal. |
| 10 | R1/B1 | HMAC (symmetric) receipts, not public-key signatures. | Matches the task's "HMAC-signed" wording; players can't self-verify but ops/regulator can decide any dispute from the persisted key, which is the dispute model B1 targets. Asymmetric receipts noted as a future hardening in security-review §1.21. |
| 11 | R1/A6 | `tsx` added as a devDependency of `@landfall/core` for the sim script. | Dev-only, already used by @landfall/server (no new supply-chain surface); needed for a node-runnable TS script without a build step. |
| 12 | R1/A6 | Sim's 10M-run measured values written into math-model §7 / simulation-methodology §2 with run date and seed. | A6 "doc tables show the new measured values"; date+seed keep the numbers auditable and reproducible. |
| 13 | R2/D1 | Primary reads label `ANCHORED` with sub-caption `{cove} · {stake}` (two lines) instead of one line `ANCHORED — {cove} · {stake}`. | Fits the 16px-primary/12px-caption typography law without truncation at min-w-52; same content, same calm confirmed state. |
| 14 | R2/D1 | RESTAKE renders in a RESERVED slot (invisible+inert when stake matches) rather than mounting/unmounting. | D1 requires the primary's position and size never change within a phase; a reserved slot keeps deck geometry constant. |
| 15 | R2/D2 | WreckLog 18px history chips kept sub-44px. | They carry no action (informational, focusable only for screen-reader labels); the touch-target law binds interactive elements. Recorded in d2-accessibility-audit.md exemptions. |
| 16 | R2 | Grandmother protocol (§13.6) NOT run — requires 5 human participants 55+. | Cannot be executed by the engineering agent; flagged as an open human gate for after Phase R2 and again after R4. |
| 17 | R2/D4 | Indicator copy: `If another cove is hit: ≈ +a–b% · heaviest cove: up to ≈ +c%`, where a–b spans the non-heaviest other coves. | Matches the README's example rendering; heaviest is split out so the "up to" never reads as typical. Band ratio representatives in `TIDE_BAND_RATIO_ESTIMATE` (core), unit-tested by hand-computed arithmetic. |
| 18 | R3/B5 | Whale cap evaluated against the handle INCLUDING the candidate stake (stake ≤ f×(others+stake), i.e. ≤ others/3 at 25%). | "May not exceed 25% of the current public handle" is ambiguous about the candidate's own inclusion; including it gives the cleaner meaning ("you can never BE more than a quarter of the room") and a stricter, monotone bound. |
| 19 | R3/B5 | Client pre-check estimates the handle from the last exact lock snapshot (previous round), not the live tide bands. | Live bands intentionally hide exact totals (Identity Freeze §2.5); the last published lock snapshot is public, exact, and a good-enough estimate to prevent surprise rejects. Server stays authoritative. |
| 20 | R3/C1 | Round ids stay globally unique across rooms (single autoincrement `rounds` table with a `room_id` column). | Keeps /api/round/:id, receipts and verification records stable and room-agnostic; per-room sequences would break existing verify paths for zero benefit. |
| 21 | R3/C1 | Switching rooms cancels + refunds any live order in the old room automatically. | A player cannot hold orders in two rooms at once (would double their information surface); refund-on-switch is the only non-destructive default. |
| 22 | R3/C5 | Bot exclusion from Golden Anchor implemented via an `isBot` flag on public lock-snapshot entries. | Verification recomputes the winner from the snapshot alone (Identity Freeze §2.3); the flag makes the exclusion recomputable and honest rather than a hidden server-side filter. |
| 23 | R3/C2 | Default tiers: Skiff 1–50 (seed 25), Schooner 5–500 (seed 50), Flagship 50–5,000 credits (seed 250); bots only in Skiff/Schooner. | "Casuals never share a room with 100× their stake": each tier's max is ≤100× its min with whale caps at 25%; Flagship stays bot-free even in demo to exercise the production path. |
| 24 | R3/B4 | Flag cooldown key: rounds where a flag was ACCEPTED, tracked in-memory per coordinator (resets on server restart). | Cooldown is behavioral friction, not money state — persisting it would be over-engineering; a restart forgiving a flag round is harmless. Client mirrors it optimistically for the dimmed-flag counter UI. |
